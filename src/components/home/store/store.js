import React from 'react';
import OrderDetails from './order_details';

const Store = (props) => {
  return (
      <div>
        Store
        <OrderDetails />
      </div>
  );
};

export default Store;
