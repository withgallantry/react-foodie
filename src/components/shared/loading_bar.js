import React from 'react';

const div = {
  textAlign: 'center',
};

const LoadingBar = () => {
  return (
    <div style={div}>
      <img src='img/loading.gif' />
    </div>
  );
};

export default LoadingBar
