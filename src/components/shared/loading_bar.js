import React from 'react';

const STYLE = {
  textAlign: 'center',
};

const LoadingBar = () => {
  return (
    <div style={STYLE}>
      <img src='img/loading.gif' />
    </div>
  );
};

export default LoadingBar
